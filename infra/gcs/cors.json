[
  {
    "// NOTE": "Edit 'origin' to match EXACTLY the Netlify site origin(s) the browser uploads from. These must be scheme+host(+port) with NO trailing slash and NO path. The browser PUTs the file directly to the signed URL, so these origins must be allowed by bucket CORS or the upload will fail the preflight. Remove the localhost entry for production-only buckets.",
    "origin": [
      "https://apolaki.com",
      "https://apolaki.netlify.app",
      "https://REPLACE-WITH-YOUR-DEPLOY-PREVIEW.netlify.app",
      "http://localhost:5173"
    ],
    "method": ["PUT", "GET", "HEAD", "OPTIONS"],
    "responseHeader": [
      "Content-Type",
      "Content-MD5",
      "Authorization",
      "x-goog-resumable",
      "x-goog-content-length-range"
    ],
    "maxAgeSeconds": 3600
  }
]
