# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://apolaki_user:your_secure_password@localhost:5432/apolaki_solar

# bcrypt cost factor for demo user passwords (lower = faster seeding)
SEED_BCRYPT_ROUNDS=10
