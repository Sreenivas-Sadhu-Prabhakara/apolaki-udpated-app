# Frontend environment variables for production build
# These are used by Vite at build time

# API URL — set to Cloud Run backend URL after deployment
VITE_API_URL=/api

# Static asset base URL (GCS bucket for images, CSS, etc.)
# Leave empty to use relative paths, or set to GCS URL
VITE_ASSET_BASE_URL=
