You are the MAKER agent migrating the Apolaki Solar Platform from Netlify to GCP Free Tier.

PROJECT ROOT: /Users/macstudio/Documents/Code/apolaki-udpated-app
CURRENT STEP: 0.1 — Create git branch gcp/phase-0-setup

INSTRUCTIONS:
1. Read the migration plan: cat /Users/macstudio/Documents/Code/apolaki-udpated-app/scripts/gcp-migration/CLAUDE.md
2. Execute ONLY step 0.1: Create git branch gcp/phase-0-setup
3. Make the SMALLEST possible atomic change — do not touch anything outside this step's scope
4. Preserve all existing functionality (backwards compatible)
5. If this is a 'Create git branch' step: run 'git checkout -b <branch-name>' from main
6. If this is a 'Commit & merge' step: stage changes, commit with message 'gcp-migrate(phase-N): step 0.1 - Create git branch gcp/phase-0-setup'
7. Test your changes if possible (run build, lint, or verify command output)
8. After making changes, create a summary report file at: /Users/macstudio/Documents/Code/apolaki-udpated-app/scripts/gcp-migration/steps/0.1/MAKER_REPORT.md
   The report MUST contain these sections:
   ## Changes Made
   - list each file changed and what was done
   ## Commands Run
   - list each command and its result
   ## Verification
   - how to verify the step worked (with pass/fail)
   ## Risk Assessment
   - LOW/MEDIUM/HIGH with reasoning
   ## Rollback
   - exact command to undo (e.g. git checkout -- file, git revert HEAD)

CRITICAL RULES:
- Do NOT proceed to any other step
- Do NOT modify files outside the scope of step 0.1
- Do NOT delete existing working code — rename to .archived or comment with // GCP-MIGRATION: disabled
- If you cannot complete the step, explain why in the report and stop
